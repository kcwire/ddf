<!--
/**
 * Copyright (c) Codice Foundation
 *
 * This is free software: you can redistribute it and/or modify it under the terms of the GNU Lesser General Public License as published by the Free Software Foundation, either
 * version 3 of the License, or any later version. 
 *
 * This program is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
 * See the GNU Lesser General Public License for more details. A copy of the GNU Lesser General Public License is distributed along with this program and can be found at
 * <http://www.gnu.org/licenses/lgpl.html>.
 *
 **/
 -->
<features name="${project.artifactId}-${project.version}"
          xmlns="http://karaf.apache.org/xmlns/features/v1.3.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.3.0 http://karaf.apache.org/xmlns/features/v1.3.0">

    <repository>mvn:org.apache.karaf.features/enterprise/${karaf.version}/xml/features</repository>
    <repository>mvn:org.apache.karaf.features/framework/${karaf.version}/xml/features</repository>

    <feature name="kernel" version="${project.version}">
        <feature version="${karaf.version}">framework</feature>
        <!-- Karaf forgot to list the version for wrap -->
        <feature>wrap</feature>
        <feature version="${karaf.version}">aries-blueprint</feature>
        <feature version="${karaf.version}">shell</feature>
        <feature version="${karaf.version}">shell-compat</feature>
        <feature version="${karaf.version}">feature</feature>
        <feature version="${karaf.version}">jaas</feature>
        <feature version="${karaf.version}">ssh</feature>
        <feature version="${karaf.version}">management</feature>
        <feature version="${karaf.version}">bundle</feature>
        <feature version="${karaf.version}">config</feature>
        <feature version="${karaf.version}">deployer</feature>
        <feature version="${karaf.version}">diagnostic</feature>
        <feature version="${karaf.version}">feature</feature>
        <feature version="${karaf.version}">instance</feature>
        <feature version="${karaf.version}">kar</feature>
        <feature version="${karaf.version}">log</feature>
        <feature version="${karaf.version}">package</feature>
        <feature version="${karaf.version}">service</feature>
        <feature version="${karaf.version}">system</feature>
        <capability>kernel</capability>
    </feature>

    <feature name="kernel-webconsole" version="${project.version}">
        <feature version="${project.version}" prerequisite="true">kernel</feature>
        <feature version="${karaf.version}" prerequisite="true">webconsole</feature>
    </feature>

    <feature name="kernel-webconsolebranding" description="DDF Kernel-customized karaf webconsole"
             version="${project.version}">
        <feature version="${project.version}" prerequisite="true">kernel-webconsole</feature>
        <bundle>mvn:commons-io/commons-io/${commons-io.version}</bundle>
        <bundle>mvn:ddf.distribution/webconsole-branding/${project.version}</bundle>
        <bundle>mvn:ddf.distribution/webconsole-branding-plugin/${project.version}</bundle>
    </feature>

</features>
